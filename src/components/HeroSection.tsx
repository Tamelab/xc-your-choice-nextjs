import { Button } from "react-bootstrap";

export default function HeroSection() {
  return (
    <div className="hero">

      <div className="hero-content">

        <h1>AIR MAX 95</h1>

        <p>Above the Influence</p>

        <div className="hero-buttons">
          <Button className="nike-btn">Shop</Button>
          <Button className="nike-btn">Watch ▶</Button>
        </div>

      </div>

    </div>
  );
}